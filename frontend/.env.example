VITE_API_BASE_URL=http://localhost:8000
VITE_HANDBOOK_URL=http://127.0.0.1:8001/
# Google OAuth web client used only for the protected teacher-document workflow.
# This is an OAuth client ID (not a secret) and must match the backend configuration.
VITE_GOOGLE_DOCUMENTS_CLIENT_ID=
